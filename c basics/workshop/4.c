#include<stdio.h>

int main(){
    int a,b;
    printf("enter the value of a and b\n");
    scanf("%d %d",&a,&b);
    printf("\nthe value of square is %d %d ",a*a,b*b);
    printf("\nvalue of cube  %d %d",a*a*a,b*b*b);
    return 0;
}