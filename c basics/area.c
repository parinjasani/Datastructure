#include<stdio.h>
void main()
{
    int length;
    int area;
    printf("enter the the length of square ");
    scanf("%d",&length);
    area=length*length;
    printf(" the area of the sqaure is equal to =%d ",area);
}